version: "3.4"

services:
  mysql:
    image: mysql:5.7
    container_name: testAssignment_mysql
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: "true"
  app:
    image: maximfox/testassignment-php:1.0
    container_name: testAssignment_app
    environment:
      APP_KEY:
      DB_CONNECTION: mysql
      DB_HOST: mysql
      DB_PORT: 3306
      DB_DATABASE: laravel
      DB_USERNAME: root
      DB_PASSWORD:
    ports:
      - "8080:80"
    command: ["./wait_for_db.sh"]
    depends_on:
      - mysql
